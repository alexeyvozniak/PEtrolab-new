# Milestone policy

A milestone build is a build intended for real user evaluation, not every successful commit artifact. It requires the complete normative real-workbook gate in addition to public CI.

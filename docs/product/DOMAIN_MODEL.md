# Domain Model

## Принцип

Сущности имеют самостоятельный научный смысл. Экран может показывать их по-разному, но не создаёт новый смысл данных. Интерфейс обращается к прикладным операциям, а не к таблицам базы напрямую.

## Базовые сущности

| Сущность | Смысл | Не является |
|---|---|---|
| Project | локальное исследовательское пространство, настройки и история | одним Excel-файлом |
| Source | происхождение данных: публикация, архив, лабораторный файл, полевая запись; для публикации хранит библиографию, DOI и/или внешнюю ссылку | только фильтром на графике или обязательной локальной копией PDF |
| Import Batch | один зафиксированный запуск импорта с правилами и отчётом | Source целиком |
| Import Recipe | воспроизводимое описание распознавания листов/блоков, колонок, единиц и Measurement Semantics Decisions | самим импортированным Dataset или догадкой UI |
| Dataset Sheet | лист или логический набор внутри импорта | Sample |
| Sample | физический образец с устойчивым идентификатором, полевым контекстом и местом хранения | строкой анализа или фотографией |
| Thin Section | шлиф, его изображение и пространственные аннотации | Sample, хотя может быть с ним связан |
| Spatial Annotation | физическая привязка на изображении шлифа: Point, Rectangle или Square с координатами и геометрией | Analysis, Media Asset или произвольной меткой на экране |
| Analytical Point | научная связь нескольких Analyses, относящихся к одной точке, зерну, зоне или повторному измерению | новым Analysis, средним значением или обязательной отметкой на изображении |
| Analysis | одна аналитическая запись/точка с идентификатором, методом и provenance | средним значением или графической точкой |
| Measurement | измеренное значение компонента с единицей, пределом обнаружения и статусом | автоматически пересчитанным значением |
| Derived Value | результат явного преобразования или расчёта | исходным измерением |
| Scientific Method Definition | версионированное определение формулы/алгоритма, входного домена, допущений, применимости, источника и benchmark cases | кнопкой или текстом интерфейса |
| Edit Draft | несохранённая версия явных правок с исходным fingerprint и состоянием конфликта | Measurement или Operation Journal |
| Interpretation | научная классификация, комментарий или вывод пользователя | измерением или Derived Value |
| Mineral | контролируемая таксономическая сущность минерала | свободным текстом в строке |
| Rock Type | контролируемая сущность породы и её классификационное положение | свободным текстом Sample или Mineral |
| Rock Assignment | версионируемое назначение Rock Type образцу с методом, автором, уверенностью и основанием | перезаписью исходного названия породы |
| Generation | именованная проектная классификация происхождения/зоны анализа с владельцем и историей | цветом, фильтром или незафиксированной подписью |
| Selection | текущее эфемерное точное множество Analysis, выбранное пользователем | фильтром, поисковым запросом или Work Group |
| Work Group | сохранённое именованное множество Analysis с описанием, автором и временем создания | текущим Selection |
| Filter | обратимое правило видимости в конкретном контексте | удалением данных или Selection |
| Table View | сохранённые столбцы, порядок, сортировка, группировка и Filter таблицы | Selection, Work Group или копия Dataset |
| Plot Specification | сохранённая настройка визуального представления: оси, стили, фильтры, слои | самим набором данных |
| Plot Layer | упорядоченный слой внутри Plot Specification с точным входным набором, стилем точек и опциональным Field | Selection, Work Group или удалением данных |
| Chart | созданное представление по Plot Specification и определённому входному набору | постоянной копией Analysis |
| Calculation Run | воспроизводимый запуск научного метода с входом, параметрами и версией | интерфейсной кнопкой |
| Workspace Snapshot | снимок конкретного Data Universe, таблицы, панелей и видимости для продолжения работы | переносимым на другие данные шаблоном |
| Analysis Template | переиспользуемая структура таблицы/панелей/методов без конкретных Analysis ID | Workspace Snapshot конкретного исследования |
| Operation Journal Entry | запись обратимого или необратимого действия с точным scope и последствиями | логом UI без доменного смысла |
| Media Asset | неизменяемый исходный файл изображения/архива, его тип и метаданные | Thin Section, Analysis или экспортированной копией с метками |
| Sample Note | версионируемая заметка об образце с автором и временем | заменой полевого описания или Measurement |
| Media Export | производный файл без меток либо rendered-копия с выбранными аннотациями и параметрами оформления | изменением исходного Media Asset |
| Media Link | обратимая связь Media Asset с Point или областью Spatial Annotation | перемещением или копированием медиафайла |
| Publication Package | экспортируемый набор графиков, таблиц, описаний и provenance | публикацией-Source |

## Ключевые отношения

- Project содержит Sources, Import Batches, Samples, Analyses, Work Groups, Plot Specifications и Calculation Runs.
- Import Batch создаёт или связывает Dataset Sheets, Sources и Analyses; все преобразования импорта фиксируются в provenance.
- Import Batch ссылается на неизменяемый снимок Import Recipe. Изменение recipe создаёт новую версию и не меняет уже импортированные Analyses.
- Sample может иметь много Analyses, Thin Sections, Media Assets и Sample Notes. Полевое местонахождение и физическое место хранения являются разными полями.
- Sample может иметь исходное название породы и одно или несколько Rock Assignments. Текущее принятое назначение показывается отдельно от импортированного текста.
- Analysis явно имеет область измерения: `mineral`, `whole-rock` или другой контролируемый тип. Whole-rock Analysis относится к Sample и через его Rock Assignment доступен на экранах породы; минералогический Analysis не становится анализом породы автоматически.
- Analysis принадлежит ровно одному первичному Source и может иметь дополнительные библиографические связи; содержит одну или несколько Measurements и Derived Values.
- Analysis может быть привязан к нулю или одному Sample и входить в ноль или несколько Analytical Points. Отсутствие связи не является ошибкой.
- Analytical Point связывает два или более Analyses устойчивыми идентификаторами, хранит тип связи и может ссылаться на ноль или одну Spatial Annotation. Одна Spatial Annotation может связывать несколько Analyses разных методов через Analytical Point.
- Spatial Annotation принадлежит одному Thin Section, хранит геометрию `point`, `rectangle` или `square`, координаты в системе изображения и, при калибровке, в системе шлифа.
- Media Asset может быть связан с нулём или несколькими Spatial Annotations через Media Link. Связь с Point или областью обратима и не изменяет исходный файл.
- Media Export ссылается на исходный Media Asset и сохраняет режим, выбранные Spatial Annotation, подписи, масштаб, легенду, параметры рендера и время создания.
- Selection и Work Group содержат ссылки на Analysis по устойчивому идентификатору, а не по номеру видимой строки таблицы.
- Plot Specification ссылается на входной набор через Work Group, явно сохранённый список Analysis или список Analytical Points и хранит Filter как отдельную настройку представления. Для Analytical Point он также хранит источник каждого признака и явное правило разрешения повторов.
- Plot Layer хранит Analysis ID, z-order, стиль/видимость точек и спецификацию Field. Field ссылается на точный вход и версионированный метод; скрытие маркеров не меняет этот вход.
- Derived Value и Calculation Run ссылаются на Scientific Method Definition и fingerprints входов. Workspace Snapshot ссылается на существующие сущности и не создаёт их копии.
- Analysis Template не содержит конкретные Analysis ID, Source ID или Dataset ID. Применение шаблона создаёт новую Plot Specification/Table View только после проверки совместимости полей и единиц.

## Инварианты

1. `Selection` временный; его нельзя считать сохранённой выборкой.
2. `Work Group` сохраняет состав на момент создания. «Умные группы» на основе запроса могут появиться позже только как отдельный тип с явным снимком результата.
3. Результат кластеризации хранится внутри `Calculation Run` как соответствие устойчивых Analysis ID вычисленным меткам кластера и, при наличии, вероятностям/расстояниям. Метка кластера не является `Generation`, `Mineral`, QC-решением или `Work Group`.
4. Команда «Сохранить кластер как рабочую группу» создаёт обычный снимок `Work Group` и ссылку provenance на исходный `Calculation Run`. Повторный расчёт не меняет созданную ранее группу.
5. Выключение `Source` на графике создаёт/меняет только `Filter`; Source и его Analyses не удаляются.
6. `Generation` имеет проектный словарь значений, автора изменения и историю. Она не выводится автоматически из цвета или положения точки.
7. Каждое исходное значение имеет метод, единицу и provenance. `F не измерен` не равно `F = 0`.
8. Derived Value всегда хранит правило, параметры, версию алгоритма и ссылки на входные Measurement.
9. Фокус на Spatial Annotation не создаёт и не изменяет Selection. Добавление связанных Analyses в Selection является отдельным явным действием.
10. Точка, поставленная при большом увеличении, хранит исходные координаты, а не только положение текущего viewport.
11. Analytical Point не изменяет и не объединяет исходные Measurement. Построение по ней обязано хранить contributing Analysis ID для каждого показанного значения.
12. Несколько значений одного признака внутри Analytical Point требуют явного выбора Analysis или версионированного правила агрегации; молчаливое усреднение запрещено.
13. Автоматическая связь допустима только для однозначного составного ключа `Sample ID + normalized point name` и только по явно включённому правилу проекта/import recipe. Исходное имя, версия нормализатора, уверенность и история отмены сохраняются.
14. Похожее имя точки, совпадение имени в разных Samples или несколько кандидатов одного метода не создают Analytical Point автоматически.
15. Совпадение Sample у Media Asset и Analytical Point только сужает список кандидатов. Пространственное положение создаётся ручным действием пользователя; автоматическое размещение по имени или Sample запрещено.
16. Связь Analytical Point с изображением другого Sample допустима только после явного подтверждения и сохраняет признак исключения в provenance.
17. Исходный Media Asset неизменяем. Экспорт без меток копирует исходные пиксели, а экспорт с метками создаёт новый производный файл; ни один режим не перезаписывает источник.
18. Наличие Spatial Annotation не является условием экспорта Media Asset.
19. Исходное название породы из файла сохраняется в provenance. Назначение или пересмотр Rock Type не меняет исходную строку Analysis или Sample.
20. Source-публикация не требует хранения PDF. Библиографическая запись, DOI и внешняя ссылка достаточны; недоступность ссылки не удаляет Source и его Analyses.
21. `show_points = false` является стилем Plot Layer и не равно Plot Visibility. Plot Visibility исключает Analysis из визуализации и расчёта Field; стиль только скрывает маркеры.
22. Field без сохранённых Analysis ID, метода и параметров считается невоспроизводимым и не может входить в Publication Package без предупреждения.
23. Import Recipe и Scientific Method Definition версионируются. Изменение определения не переписывает ранее импортированные Analyses или Calculation Runs.
24. Edit Draft не является Measurement. Сохранение требует повторной проверки fingerprint источника и создаёт Operation Journal Entry.
25. Workspace Snapshot может ссылаться на конкретные Analysis ID, а Analysis Template не содержит конкретных Analysis, Source или Dataset ID.
26. Восстановление Import Recipe, Plot Specification, Workspace Snapshot или Analysis Template выполняется по версии схемы и semantic fingerprint. Любая миграция фиксирует изменённые/отброшенные поля; silent partial restore запрещён.

## Архитектурные границы

- **Scientific core:** доменная модель, валидация, расчёты, импортные преобразования, provenance, экспорт.
- **Persistence:** SQLite и миграции; хранит модель без знания о вкладках, виджетах и библиотеках графиков.
- **Application services:** операции `import`, `select`, `save_work_group`, `filter_source`, `build_chart`, `create_spatial_annotation`, `link_analysis_to_annotation`, `link_media_to_annotation`, `run_calculation`, `export_package`.
- **UI:** отображает состояние и вызывает application services. В UI не живут формулы, правила классификации или SQL-логика.
- **Desktop shell:** запуск, файловые диалоги, обновление, Windows-инсталлятор. Не владеет научными сущностями.

name: Import regression corpus

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "fixtures/import/**"
      - "src/petrolab/import_*.py"
      - "src/petrolab/import/**"
      - "tests/test_import_regression_corpus.py"
      - "tests/real_world_fixtures.py"
      - ".github/workflows/import-regression.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  corpus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run immutable import regression corpus
        run: python -m unittest discover -s tests -p "test_import_regression_corpus.py" -v
